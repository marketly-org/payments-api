apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: marketly
  labels:
    app: payments-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: payments-api
